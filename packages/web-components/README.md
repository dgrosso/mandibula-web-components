# @mandibula/web-components

Convenience package that registers the complete collection. Prefer the individual `@mandibula/*` packages when bundle size and dependency isolation matter.

```js
import "@mandibula/web-components";
```
